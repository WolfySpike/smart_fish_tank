# 植物补光灯控制系统开发计划（STM32F103C8T6）

> 历史计划：本文记录早期设想，不代表当前已实现的功能。现有固件为鱼缸自动喂食与照明系统，补光采用 GPIO 定时开关，尚未接入光敏闭环调光、串口日志或菜单水位阈值设置。当前功能与接线请以 [README](README.md) 和 [硬件说明](docs/HARDWARE.md) 为准。

 ## 1. 项目目标

 实现一个可演示、可答辩的植物补光灯控制系统，具备以下功能：

 - 基于 DS1302 的定时补光
 - 基于光敏传感器的光照闭环调光（PWM）
 - 基于水位传感器的低水位报警
 - 通过按键进行参数设置（时间段、目标光照、水位阈值）
 - OLED 实时显示运行状态与参数

 ---

 ## 2. 开发路线选择

 - 开发方式：CubeMX + HAL
 - 理由：外设初始化效率高、联调更快、课程设计周期内更稳妥

 > 备注：若老师强制要求标准库，再切换到标准库实现。

 ---

 ## 3. 硬件清单（已具备）

 - STM32F103C8T6 最小系统板
 - 红 / 黄 / 绿 指示灯（用于水位高/中/低）
 - 白 LED1（鱼缸灯）
 - 白 LED2（补光灯，新增一盏）
 - DS1302 时钟模块
 - SG90 舵机（喂食执行）
 - 水位传感器模块（模拟输出）
 - 光敏电阻传感器模块（模拟/数字输出）
 - 贴片按键若干
 - 1.3 寸 OLED
 - 面包板与杜邦线若干

 ---

 ## 4. 推荐引脚分配

 | 模块 | 信号 | 引脚 | 说明 |
 |---|---|---|---|
 | OLED(软件I2C) | SCL | PB8 | GPIO 开漏输出 |
 | OLED(软件I2C) | SDA | PB9 | GPIO 开漏输出 |
 | DS1302 | RST/CE | PB12 | GPIO 输出 |
 | DS1302 | SCLK | PB13 | GPIO 输出 |
 | DS1302 | DAT/IO | PB14 | 双向 GPIO（软件模拟） |
 | 光敏传感器(AO) | ADC | PA1 | ADC1_IN1 |
 | 水位传感器(AO) | ADC | PA5 | ADC1_IN5 |
 | 水位指示灯 R | PWM | PA6 | TIM3_CH1 |
 | 水位指示灯 Y | PWM | PA7 | TIM3_CH2 |
 | 水位指示灯 G | PWM | PB0 | TIM3_CH3 |
 | 白 LED1（鱼缸灯） | PWM | PB1 | TIM3_CH4 |
 | 白 LED2（补光灯） | 输出 | PB6 | GPIO 推挽输出（定时开关） |
 | 按键 KEY0 | 输入 | PA0 | 上拉输入 |
 | 按键 KEY1 | 输入 | PA2 | 上拉输入 |
 | 按键 KEY2 | 输入 | PA4 | 上拉输入 |
 | SG90 舵机 | PWM信号 | PA8 | TIM1_CH1，50Hz |
 | 串口调试（可选） | TX/RX | PA9/PA10 | USART1 |

 > 说明：补光灯只做“定时开/关”时，PB6 作为普通 GPIO 即可。若后续希望补光灯也调亮度，可改到支持 PWM 的通道。

 ---

 ## 5. CubeMX 配置清单

 ### 5.1 时钟

 - HSE 外部晶振
 - SYSCLK = 72 MHz
 - Debug: Serial Wire

 ### 5.2 GPIO

 - DS1302 三线先配置为 GPIO 输出（IO 在驱动中动态切换输入/输出）
 - OLED 的 PB8/PB9 配置为 `GPIO_Output Open-Drain`
 - 按键 PA0/PA2/PA4 配置为 `Input Pull-up`
 - 新增补光灯 PB6 配置为 `GPIO_Output Push-Pull`

 ### 5.3 ADC1

 - 通道：IN1(PA1)、IN5(PA5)
 - 对齐：Right
 - 采样时间建议：55.5 cycles（先保证稳定）

 ### 5.4 TIM3 PWM（水位灯 + 鱼缸灯）

 - CH1~CH4 全开（PA6/PA7/PB0/PB1）
 - 目标频率 1 kHz
 - 推荐参数：Prescaler = 72-1，Period = 1000-1

 ### 5.5 TIM1 PWM（SG90）

 - CH1(PA8) 50Hz
 - 推荐参数：Prescaler = 72-1，Period = 20000-1
 - Pulse 初始设 1500（中位）

 ### 5.6 I2C1

 - 当前不启用硬件 I2C1（OLED 使用软件 I2C）

 ### 5.7 USART1（可选）

 - 115200，用于日志打印与联调

 ### 5.8 新增白灯硬件建议

 - PB6 不要直接带大功率灯，建议用三极管或 MOSFET 驱动
 - 灯珠串联限流电阻，和主控共地
 - 若灯带电流较大，单独供电并确保地线共地

 ---

 ## 6. 软件架构

 ```text
 Core/Src
   main.c
   app_scheduler.c     // 10ms 调度
   app_control.c       // 补光策略
   app_menu.c          // 按键菜单
   app_display.c       // OLED 页面
   drv_ds1302.c
   drv_oled.c
   drv_key.c
   drv_adc.c
   drv_led_pwm.c
 ```

 ---

 ## 7. 控制策略（MVP）

 1. 读取 DS1302 当前时间
 2. 若在补光时段内：
    - 读取光敏 ADC
    - 按误差计算 PWM（比例控制）
 3. 若不在补光时段：
    - 灯关闭（PWM=0）
 4. 读取水位 ADC：
    - 低于阈值时触发报警并 OLED 提示

 比例控制建议：

 ```c
 error = target_light_adc - light_adc;
 pwm = Kp * error;
 pwm = clamp(pwm, 0, PWM_MAX);
 ```

 ---

 ## 8. OLED 页面规划

 - 主页面：时间、光敏值、水位值、PWM、模式
 - 参数页面：开始时间、结束时间
 - 阈值页面：目标光照、水位报警阈值

 ---

 ## 9. 开发排期（单人执行）

 ### Day 1

 - 新建 CubeMX 工程
 - 跑通 GPIO / ADC / PWM / I2C / 串口基础功能

 ### Day 2

 - 完成 DS1302 驱动与时间读写
 - OLED 显示文本
 - 按键消抖与扫描

 ### Day 3

 - 接入补光自动控制逻辑
 - 接入水位报警逻辑
 - 完成主页面显示联动

 ### Day 4

 - 完成参数菜单设置
 - 做参数范围限制和默认值管理

 ### Day 5

 - 全流程联调与参数标定
 - 设计答辩演示场景（正常/低光/低水位/超时段）
 - 整理文档与流程图

 ---

 ## 10. 关键风险与规避

 1. 传感器模拟输出可能超过 3.3V
    - 规避：上电前先测 AO 电压，必要时分压

 2. LED 负载电流过大
    - 规避：使用三极管或 MOSFET 驱动，避免 IO 口直驱大电流

 3. 传感器噪声导致控制抖动
    - 规避：ADC 多次采样均值 + PWM 变化限幅

 ---

 ## 11. 验收标准（课程设计）

 - 可设定补光时间段并按时启停
 - 可根据光照变化自动调节亮度
 - 可识别低水位并给出明显报警提示
 - OLED 信息完整，按键可稳定设置参数
 - 系统连续运行稳定，无明显死机/误触发

 ---

 ## 12. 下一步

 - 建立 CubeMX 工程
 - 先完成“ADC + PWM + OLED 主页面”最小闭环
 - 再接入 DS1302 与按键菜单
